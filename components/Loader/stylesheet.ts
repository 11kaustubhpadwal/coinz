import { StyleSheet } from "react-native";

export const LoaderStyles = StyleSheet.create({
  main: {
    width: 75,
    height: 75,
    marginLeft: "auto",
    marginRight: "auto",
  },
});
