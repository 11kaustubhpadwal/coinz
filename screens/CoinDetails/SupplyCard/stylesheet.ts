import { StyleSheet } from "react-native";

export const SupplyCardStyles = StyleSheet.create({
  wrapper: {
    padding: 15,
    backgroundColor: "white",
    borderRadius: 8,
    marginBottom: 8,
    elevation: 1,
  },
});
