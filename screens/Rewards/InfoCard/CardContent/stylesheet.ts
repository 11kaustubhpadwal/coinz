import { StyleSheet } from "react-native";

export const CardContentStyles = StyleSheet.create({
  wrapper: {
    flexDirection: "row",
    marginBottom: 12,
  },
  column1: { flex: 1 },
});
